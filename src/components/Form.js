import React from 'react';
import PropTypes from 'prop-types';

const Form = props => {
    return (
        <Form>
            
        </Form>
    );
};

Form.propTypes = {
    
};

export default Form;